"""JaSketch MCP server: drive a live JaSketch canvas from an AI assistant.

Published to PyPI as `jasketch-mcp-server` from mcp_server/jac.toml. The wheel
is plain transpiled Python with a vendored jac runtime, so it installs and runs
under `uvx` with no jac toolchain present.
"""
