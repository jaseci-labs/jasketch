[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "jasketch-mcp-server"
version = "1.1.1"
description = "MCP server for JaSketch - programmatic diagram creation"
requires-python = ">=3.12"
dependencies = [
    "jaclang",
    "mcp>=1.0.0",
    "websockets>=12.0",
    "uvicorn>=0.30.0",
]

[project.scripts]
jasketch-mcp-server = "jasketch_mcp.__main__:run_server"
jasketch-mcp-server-http = "jasketch_mcp.__main__:run_server_http"

[tool.setuptools.package-data]
jasketch_mcp = ["*.jac"]
